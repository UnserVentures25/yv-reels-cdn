name: Evaluate Trial Reels

on:
  schedule:
    - cron: "17 8 * * *"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: evaluate-trial-reels
  cancel-in-progress: false

jobs:
  evaluate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install requests
      - run: python3 automation/evaluate_reels.py
        env:
          IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}
      - name: Commit trial state
        run: |
          git config user.email "actions@github.com"
          git config user.name "github-actions"
          git add trial_state.json best_reel_pool.json
          git diff --staged --quiet || git commit -m "Update trial reel evaluation state [skip ci]"
          git pull --rebase origin main
          git push
